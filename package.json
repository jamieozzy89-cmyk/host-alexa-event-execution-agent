{
  "name": "host-alexa-event-execution-agent",
  "version": "0.2.0",
  "private": false,
  "type": "module",
  "description": "Authoritative execution engine for the Host Alexa+ hackathon simulation.",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.mjs",
    "verify": "npm test"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
