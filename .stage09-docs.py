from pathlib import Path

# README
p = Path('README.md')
s = p.read_text()
s = s.replace('**Current verified stage:** Stage 07 — voice + touch interaction', '**Current verified stage:** Stage 09 — hardened voice + touch product; hosted deployment still pending')
s = s.replace('Stage 02–07 suite: **74 tests passed, 0 failed.**', 'Stage 02–09 backend/application suite: **74 tests passed, 0 failed.**')
s = s.replace('Stage 06 + Stage 07 browser suite: **14 tests passed, 0 failed.**', 'Stage 06–09 browser suite: **26 tests passed, 0 failed.**')
s = s.replace('The 14 cases consist of:\n\n- eight retained Stage 06 touch cases;\n- six Stage 07 voice cases (three voice scenarios in each viewport).', 'The 26 cases retain the Stage 06 touch and Stage 07 voice journeys and add Stage 09 accessibility, focus, recovery, privacy/storage and network-boundary checks across the Echo Show-like and mobile projects.')
s = s.replace('Final successful Stage 07 GitHub Actions run: `33552445789`.\n\nSee `reports/STAGE07_VERIFICATION.md` for the complete acceptance record and `docs/VOICE_INTERACTION_STAGE07.md` for the voice architecture.', 'Stage 07 voice release evidence remains in `reports/STAGE07_VERIFICATION.md`. Stage 09 hardening evidence is recorded in `reports/STAGE09_VERIFICATION.md` and `docs/HARDENING_STAGE09.md`.')
s = s.replace('Physical microphone/browser compatibility is therefore a later Stage 09 manual hardening check, not a claim made by the automated suite.', 'Physical microphone/acoustic behavior across every real browser/device remains outside deterministic automation; the Stage 09 product therefore retains explicit voice-unavailable/error fallbacks rather than claiming universal browser voice compatibility.')
marker = '## Competition mini-challenge status\n'
hardening = '''## Stage 09 hardening\n\nThe current product has been hardened without creating a second execution path. Verified additions include:\n\n- keyboard focus continuity after async rerenders;\n- labelled modal dialog with initial and return focus;\n- focused scrollable conversation region;\n- one latest-message screen-reader status announcement rather than replaying the full transcript;\n- keyboard-reachable voice-unavailable state;\n- Data & privacy explanation for browser storage and browser/platform speech recognition;\n- confirmed, scoped deletion of Host event data without clearing unrelated preferences;\n- explicit in-memory mode when persistent browser storage is blocked;\n- safe recovery when persisted event snapshots cannot be restored;\n- no-referrer document policy;\n- automated Axe WCAG A/AA checks on representative surfaces;\n- automated rejection of unsafe HTML-injection primitives and direct web/domain access;\n- zero production dependency vulnerabilities at the verified gate;\n- provider-independent relative static asset paths;\n- aligned package/lock version `0.9.0` and Node.js floor `>=22.12.0`.\n\nThe full Stage 09 browser suite passes **26/26** and the backend/application suite remains **74/74**.\n\nHosted deployment is not yet claimed. The Vercel chat deployment wrapper cannot currently accept the required target/name/file arguments through its exposed schema, and GitHub Pages is disabled and cannot be enabled with the repository workflow's normal `GITHUB_TOKEN`. The build is deployment-ready, but a public URL must not be listed until a supported deployment path is available and smoke-tested.\n\n'''
if marker not in s:
    raise SystemExit('README Stage 09 insertion marker missing')
s = s.replace(marker, hardening + marker, 1)
s = s.replace('- Node.js 22+\n', '- Node.js 22.12+\n')
s = s.replace('Run the complete Stage 07 gate after browser dependencies are installed:\n\n```bash\nnpm run verify:stage07\n```', 'Run the complete Stage 09 gate after browser dependencies are installed:\n\n```bash\nnpm run verify:stage09\n```')
start = s.find('## Current boundary\n')
if start == -1:
    raise SystemExit('README current boundary missing')
s = s[:start] + '''## Current boundary\n\nHost now has the authoritative execution engine, persistence, validated tools, controlled agent/orchestrator, polished simulated Alexa+ touch UI, browser voice path, and the verified Stage 09 accessibility/privacy/recovery/security hardening layer.\n\nThe remaining Stage 09 item is **hosted deployment and smoke testing**. The current code/build is ready for deployment, but no hosted URL is claimed while the connected deployment interfaces cannot accept the exact verified artifact or enable repository Pages.\n\nAWS Builder remains deliberately deferred. Open Source remains selected.\n\nAfter a real hosted deployment is verified—or if the submission is explicitly completed using the repository/demo access route allowed by the rules—the next controlled stage is **Stage 10 — submission artifacts**.\n'''
p.write_text(s)

# HANDOVER
p = Path('HANDOVER.md')
s = p.read_text()
s = s.replace('**Current accepted product stage:** Stage 07 — voice + touch interaction  \n**Current competition decision stage:** Stage 08 — Open Source locked; AWS Builder deliberately deferred for now\n**Next executable product stage:** Stage 09 — hardening', '**Current accepted product stage:** Stage 09 — hardened voice + touch product (deployment pending)  \n**Competition decision:** Open Source selected; AWS Builder deliberately deferred for now  \n**Next controlled action:** obtain a real hosted deployment and smoke-test it; then Stage 10 submission artifacts')
marker = '## Current simulation and capability boundaries\n'
stage9 = '''## Stage 09 — competition hardening\n\nStage 09 hardening has been implemented and verified on the isolated `stage09-hardening` branch without changing the authoritative execution architecture.\n\n### Accessibility / UX controls\n\nImplemented and verified:\n\n- composer focus returns after async typed submission;\n- Live/Activity mode changes focus their contextual heading;\n- material-action rerenders retain a useful focus context;\n- expanded shopping detail uses labelled modal-dialog semantics;\n- dialog opening focuses Close and closing returns focus to the opener;\n- scrollable conversation log is keyboard-focusable;\n- the full transcript is no longer an `aria-live` region;\n- a separate visually hidden polite status node announces only the latest assistant/status message;\n- voice-unavailable state remains keyboard reachable with `aria-disabled`;\n- existing 48 × 48 touch-target and responsive controls remain tested.\n\nAxe Playwright 4.13.0 checks representative states against WCAG A/AA rule tags. Automated Axe success is not represented as complete manual accessibility certification.\n\n### Privacy / storage controls\n\nImplemented and verified:\n\n- Data & privacy UI explains browser-local event storage;\n- the current deterministic Host core journey does not send plan data to a Host application backend;\n- browser/platform speech recognition is explicitly disclosed as potentially using a browser/platform service;\n- Host does not claim to store speech audio;\n- Host event data can be deleted from the UI through a confirmation step;\n- deletion is scoped to active-event/event-snapshot keys and preserves the tested theme preference;\n- blocked browser storage falls back to an explicit in-memory-only mode;\n- corrupt unrecoverable event snapshots are preserved, the active pointer is removed, and a visible recovery message returns the user to a usable fresh planning state.\n\n### Security / boundary controls\n\nVerified release scans reject:\n\n- `.innerHTML`;\n- `insertAdjacentHTML`;\n- `document.write`;\n- direct `HostDomainEngine` / `validateHostState` use from the web layer;\n- direct browser imports from domain/persistence/tool internals.\n\n`npm audit --omit=dev` reports zero production dependency vulnerabilities at the verified gate.\n\nA browser test also observes the deterministic event/menu/shopping/product journey and rejects unexpected cross-origin application requests. This does not claim browser/vendor speech-recognition networking is local; that boundary is disclosed separately.\n\n### Reproducibility\n\nStage 09 corrected two issues found only during source audit after functional tests passed:\n\n- package/lock root versions are aligned at `0.9.0`;\n- Node engine floor is `>=22.12.0`, matching the pinned Vite 8 build requirement on the Node 22 line.\n\nThe Vite static build now uses relative `./` asset paths for provider-independent root/subpath hosting.\n\n### Verification evidence\n\nHardening functional workflow: `33557794389`.\n\nReproducibility correction workflow: `33558676755`.\n\nPortable static-base requalification workflow: `33559155046`.\n\nFinal Stage 09 product gate after the portable-base change:\n\n- backend/application tests: **74 passed, 0 failed**;\n- production web build: passed;\n- complete Playwright browser suite: **26 passed, 0 failed**;\n- Axe representative WCAG A/AA checks: passed;\n- production dependency audit: zero vulnerabilities;\n- web injection/direct-domain boundary scans: passed.\n\nSee `docs/HARDENING_STAGE09.md` and `reports/STAGE09_VERIFICATION.md`.\n\n### Source/licence/provenance observations\n\nThe public repository remains MIT licensed. The browser application has no declared third-party runtime dependency. The current TypeScript, Vite, Playwright and Axe packages are build/test development dependencies, not claimed Alexa/AWS runtime integrations.\n\n### Competition recheck\n\nOfficial Devpost rules were rechecked on 1 September 2026. The simulated Alexa+ web route remains explicitly valid; public source/setup remains required; the public English demo video remains under three minutes; Open Source evidence requirements remain the contribution URL, repository URL, GitHub username and what/how/why description; friction logs can still add up to a 10% judging bonus.\n\n### Deployment status — unresolved external item\n\nThe current build is deployment-ready but **no hosted URL is yet verified**.\n\n- Vercel is connected, but the chat deployment wrapper's backend requires target/name/files while its exposed schema provides no way to supply those arguments.\n- GitHub Pages is disabled; first-time enablement through `configure-pages` requires a token other than normal `GITHUB_TOKEN` with administration/pages permissions, which is not available here.\n\nDo not invent or list a deployment URL. The exact production build can be recreated with `npm ci && npm run build:web`, and the build is path-portable, but deployment must be completed through a working hosting-control path and then smoke-tested before being called done.\n\n'''
if marker not in s:
    raise SystemExit('HANDOVER Stage 09 insertion marker missing')
s = s.replace(marker, stage9 + marker, 1)
s = s.replace('- Node.js 22+\n', '- Node.js 22.12+\n')
s = s.replace('Run the full Stage 07 verification sequence after browser dependencies are installed:\n\n```bash\nnpm run verify:stage07\n```', 'Run the full Stage 09 verification sequence after browser dependencies are installed:\n\n```bash\nnpm run verify:stage09\n```')
start = s.find('## Exact continuation point\n')
end = s.find('## Later controlled work\n', start)
if start == -1 or end == -1:
    raise SystemExit('HANDOVER continuation markers missing')
s = s[:start] + '''## Exact continuation point\n\nStage 09 hardening functionality is verified. The only unresolved Stage 09 item is a **real hosted deployment plus smoke test**.\n\nContinue in this order:\n\n1. use a hosting path that can receive the exact verified repository/build artifact;\n2. deploy the Stage 09 release candidate without rebuilding a different product;\n3. smoke-test load, Plan, Live, Activity, local persistence/reload, touch actions and voice fallback on the hosted URL;\n4. record the exact hosted URL/provider/deployment evidence in the verification report and README;\n5. only then call Stage 09 fully closed.\n\nIf hosting remains externally blocked, keep that item explicit and do not fabricate a URL. Stage 10 submission drafting may proceed with the deployment field marked pending, but final submission must follow the then-current competition accessibility/testing requirements.\n\nAWS Builder remains deferred unless the user later explicitly resumes the Bedrock + AgentCore Memory path.\n\n''' + s[end:]
s = s.replace('After Stage 08:\n\n- **Stage 09 — hardening**\n  - actual-browser/manual microphone and permission compatibility;\n  - accessibility audit beyond automated minimums;\n  - error/recovery matrix;\n  - security/privacy review;\n  - persistence/reload and demo regression;\n  - deployment/reproducibility;\n  - source/licence/provenance review;\n  - current competition-requirements recheck.\n\n- **Stage 10 — submission artifacts**', 'Remaining controlled work:\n\n- **Stage 09 — deployment closure**\n  - real hosted deployment;\n  - hosted smoke test;\n  - update deployment evidence.\n\n- **Stage 10 — submission artifacts**')
p.write_text(s)

# Verification report: resolve portable-base pending language with actual successful run.
p = Path('reports/STAGE09_VERIFICATION.md')
s = p.read_text()
s = s.replace('This change is requalified through the same full Stage 09 test/security gate before release. The corresponding final workflow/run must be recorded before the clean release is called complete.', 'This change was requalified through the same full Stage 09 test/security gate. Successful portable-base workflow: `33559155046`.')
s = s.replace('Stage 09 hardening functionality is accepted only after the portable-base requalification succeeds.\n\nStage 09 as a whole retains one unresolved external item until a hosted deployment can be created and smoke-tested: **deployment**.', 'The portable-base requalification succeeded. Stage 09 hardening functionality is therefore verified.\n\nStage 09 as a whole retains one unresolved external item until a hosted deployment can be created and smoke-tested: **deployment**.')
s = s.replace('This report must be updated with the portable-base workflow result and any eventual hosted deployment URL/evidence before claiming complete Stage 09 closure.', 'This report now records the portable-base success. It still must be updated with an eventual hosted deployment URL/evidence before claiming complete Stage 09 closure.')
p.write_text(s)
